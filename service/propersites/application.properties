# Database Configuration
db.url=jdbc:mysql://localhost:3306/pet_adoption
db.username=root
db.password=root
db.driverClass=com.mysql.cj.jdbc.Driver

# Connection Pool Configuration (Optional)
db.pool.maxIdle=10
db.pool.maxActive=50
db.pool.minIdle=5
db.pool.maxWait=10000

# Logging Configuration (Optional)
log.level=INFO

# Email Configuration (for notifications, optional)
email.smtp.server=smtp.example.com
email.smtp.port=587
email.username=email@example.com
email.password=emailpassword
email.from=info@pawsandhearts.com

# Application Configuration
app.welcomeMessage=Welcome to Paws & Hearts Pet Adoption!

# Session Timeout
session.timeout=30 # minutes

# Enable Debug Mode (set to false for production)
debug=true
